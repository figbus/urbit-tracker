/+  *tracker
=*  as-octs  as-octs:mimes:html
::
|_  tak=tasks
++  grad  %noun
++  grow
  |%
  ++  noun  tak
  ++  json  (tasks:enjs tak)
  ++  mime  [/application/x-urb-tracker-tasks (as-octs (jam tak))]
  --
::
++  grab
  |%
  ++  noun  tasks
  ++  mime  |=([* =octs] ;;(tasks (cue q.octs)))
  --
--